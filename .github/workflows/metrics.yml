name: Métricas do GitHub

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

permissions:
  contents: write

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          user: hmarinhoo
          template: classic
          base: header, activity, community
          config_timezone: America/Sao_Paulo
          config_locale: pt-BR
